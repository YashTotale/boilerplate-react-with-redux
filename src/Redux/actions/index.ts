//Redux Actions

/* 

Example:

export const TEST_ACTION = "TEST_ACTION";
export const testAction = (sampleValue) => ({
  type: TEST_ACTION,
  payload: { sampleValue }
});

*Take a look at reducers to see how this action is used*

*/
